<phpunit>
    <filter>
        <whitelist>
            <exclude>
                <directory suffix=".php">../../tests/phpunit</directory>
            </exclude>
        </whitelist>
    </filter>
</phpunit>

